# Backrest Environment Configuration - EXAMPLE
# Copy this file to .env and fill in your actual values
# DO NOT commit .env to version control!

# Domain Configuration
BACKREST_DOMAIN=backrest.do-it.media

# Timezone Settings
TZ=Europe/Berlin
BACKUP_TZ=Europe/Berlin

# Backup Storage Path (local storage)
# This is where restic will store backups on the local filesystem
# Change this to your backup storage location
BACKUP_STORAGE_PATH=/mnt/backups

# Optional: Basic Authentication
# Uncomment and set if you want password protection in addition to Traefik
# Generate password hash with: htpasswd -nb admin yourpassword
# Or use: echo $(htpasswd -nb admin yourpassword) | sed -e s/\\$/\\$\\$/g
# BACKREST_AUTH_USER=admin:$$apr1$$hash$$here

# Optional: Email Notifications (configure in Backrest UI)
# These are examples - actual configuration is done in the web UI
# SMTP_HOST=mail.do-it.media
# SMTP_PORT=587
# SMTP_FROM=backrest@do-it.media
# SMTP_USERNAME=backrest@do-it.media
# SMTP_PASSWORD=your_smtp_password

# Optional: Restic Repository Passwords
# You can set these as environment variables, but it's more secure
# to set them individually per repository in the Backrest UI
# RESTIC_PASSWORD=your_secure_restic_password

# Optional: Cloud Storage Credentials (if using S3, B2, etc.)
# AWS S3
# AWS_ACCESS_KEY_ID=your_access_key
# AWS_SECRET_ACCESS_KEY=your_secret_key
# AWS_DEFAULT_REGION=us-east-1

# Backblaze B2
# B2_ACCOUNT_ID=your_account_id
# B2_ACCOUNT_KEY=your_account_key

# Google Cloud Storage
# GOOGLE_PROJECT_ID=your_project_id
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/credentials.json

# Azure Blob Storage
# AZURE_ACCOUNT_NAME=your_account_name
# AZURE_ACCOUNT_KEY=your_account_key
